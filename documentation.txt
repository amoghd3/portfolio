Portfolio setup — 2026-09-07

Cloned https://github.com/amoghd3/portfolio.git into C:\Users\Blese\portfolio.
The remote started with an empty repo plus a Next.js GitHub Pages workflow.

Next.js path was abandoned. create-next-app / npm / pnpm installs hung on this
machine (slow registry, timeouts on optional Linux SWC binaries). The user asked
to ship something usable on GitHub Pages instead.

Switched to a static site with no build step:
- index.html — single-page portfolio for Amogh Donthi
- styles.css — dark editorial layout (Instrument Serif + DM Sans)
- .github/workflows/pages.yml — copies those two files into _site, uploads the
  artifact, deploys with actions/deploy-pages
- .nojekyll — keeps Pages from running Jekyll

Removed the Next.js scaffold (package.json, next.config.ts, src/, public/,
nextjs.yml) so Pages does not try to compile anything.

Live URL after pushing main and enabling Pages (GitHub Actions source):
https://amoghd3.github.io/portfolio/

Copy is based on public LinkedIn/GitHub details: Wells Fargo AI transformation,
Duck Creek intern, 10xLabs, Muse Research, UIUC CS + Advertising.

To update the page, edit index.html / styles.css and push to main.

Added scroll-margin-top on sections so in-page nav clears the sticky header.
Verified locally at http://127.0.0.1:4173/: hero, About/Experience/Work/Contact
anchors, LinkedIn/GitHub hrefs, and styles.css loading.

Pushed dc2f003 to origin/main as amoghd3 after adding the workflow OAuth scope.
Site URL: https://amoghd3.github.io/portfolio/

Redesign — 2026-09-07 (later)

Rebuilt the static site around a “signal room” look: midnight + teal + copper,
blueprint grid, Bricolage Grotesque / Figtree / IBM Plex Mono. Hero now uses
the higher-quality headshot (158KB vs 17KB), cropped tighter to the face and
saved as assets/amogh.png. Impact section leads with accomplishments (4x time
studies, ~$300k save, 200+ demos, 83% reporting cut, hackathon 1st, COO summit).

New Systems section with two interactive process diagrams:
1. Voice assistant harness — STT, transcribe, memory, conversation/turn IDs,
   background actions, response, TTS, continue, plus an orchestration rail.
2. AI governance and execution engine — intake, policy, risk gate, plan,
   sandboxed execute, human loop, audit, outcome / kill switch.

Diagram nodes highlight matching explanations on hover; steps auto-cycle.
app.js handles reveal-on-scroll and diagram state. pages.yml now copies
app.js and assets/ into _site so GitHub Pages serves the photo and scripts.

Resume correction pass — 2026-09-07

Updated copy from Amogh’s resume photos (not published on the site; no phone
or Wells Fargo email). Title is now AI Product Strategist, Charlotte. Stats use
resume metrics: 380k hours (patent-pending prompt system), $27–45M ORBIT,
4x/$300K time study, 90% Ducky AI, 83% demo validation, 100+ analysts.
Experience list now includes both Wells Fargo roles, Duck Creek, MUSE,
Venture Studio, UI7, Estagrow, and OKB Hub. Education and certs added.
Work cards cover Prompt Studio, ORBIT, Ducky AI, time study/CDSC, research
awards, and Venture Studio/UI7.

Interview redesign — 2026-09-07

Replaced the dark “signal room” theme with a light, single-accent layout for
applications: paper background, navy accent, Inter + Newsreader. Structure
follows a 30-second portfolio: hero (name, role, one differentiator, CTA to
#work), four projects (Prompt Studio featured + ORBIT, Ducky AI, time study,
research), a clean Approach section with two diagrams, a 5-item timeline,
two-paragraph About, Contact. Removed auto-cycling animations, skill logos,
and resume-dump sections. No phone or work email.

Profile photo sizing — 2026-09-07

Hero portrait is now a 132px circular frame with overflow hidden so the
headshot stays inside the box. Desktop hero column matches that width.

Pushed e05c8d8 to origin/main. Pages should redeploy at
https://amoghd3.github.io/portfolio/

Voice pass — 2026-09-07

Rewrote site copy against the 25 human-writing patterns: direct claims, no
"not X but Y", no one-line closers, no fake objections, fewer I-stacks,
straight quotes, sentence-case headings, resume numbers kept as stated.
Saved the patterns as always-on rules at ~/.cursor/rules/human-writing.mdc
and .cursor/rules/human-writing.mdc in this repo.

Work by accomplishment — 2026-09-07

Work is now four clickable nodes (Prompt Studio, ORBIT, Ducky AI, time
study tracker). Clicking a node shows that item only: a three-lane process
map, a step callout, then How it was built / How it scaled / Key features.

Maps use lane bands, numbered badges, titled nodes, and arrow connectors.
Clicking a map node highlights the matching step and updates the callout.
Approach maps (voice harness, governance engine) use the same treatment.

CSS: .acc-map, .acc-node, .work-panel[hidden], .notes 3-col, .step-card,
.diagram .band / .badge / .ttl. app.js wires tabs and map highlight.

Two built systems — 2026-09-07

Added work nodes 05 and 06. Desktop grid is now 3 columns.

05 Process map editor: requirements + business docs → generated map →
select / talk / edit → automatic diagram update → SOR connection for
control alignment. Lanes: Gather, Edit, Align.

06 Document processor: intake of PDFs and sheets → minimized working set
for semantic reasoning → standard output → HIL review → audit log on
every intake and creation step → selective RAG for later agent use.
Lanes: Intake, Generate, Govern.

No metrics invented. Kickers say Built system. Same panel pattern as the
Wells Fargo / Duck Creek items.

Work as a timeline — 2026-09-07

Work is grouped by when it was built:

2024 Duck Creek: Ducky AI RFx, Excel AI harness (new). The harness is a
VBA macro that calls an AI API and executes the returned code in the
sheet. Copy says this was 2024, ahead of Copilot in Excel.

2025 intern: Time study tracker rewritten as track → log → notate →
store in cells → tabulate selection → visualize. Scale lane still has
2-to-8, ~$300K, CDSC 5h to 3h.

2025 intern: ORBIT rewritten as architecture. Data (profile, library,
events), services (adaptive engine, mentor, manager view), ship
(capstone, POC, rollout case). Notes heading is "How it would be built".

2025 to now: Prompt Studio. Then Systems I built (map editor, document
processor).

Default selected node is Ducky (first in time). Path copy updated for
2024 and 2025 intern lines. CSS: .era, .era-label, .era-one.

Two pages — 2026-09-07

index.html is the first page: hero, four high-level results, work cards
in timeline order, path, about, LinkedIn. Cards link to work.html#id.
GitHub button removed.

work.html is the breakdown: same project nodes, process maps, notes, and
the Approach maps. Hash opens the matching panel. Back to home link.
pages.yml now copies work.html into _site. app.js reads location.hash.

Newest first + senior voice — 2026-09-07

Work on home and work.html now lists newest to oldest:
01 Prompt Studio (2025 to now)
02 Process map editor, 03 Document processor (systems I built)
04 ORBIT, 05 Time study tracker (2025 intern)
06 Ducky AI, 07 Excel AI harness (2024)

Default work panel is Prompt Studio. app.js falls back to studio when
there is no hash.

Copy tightened to hiring-manager voice without new metrics: hero states
production AI systems and the scoping-to-LOB path; cards and panel ledes
use enterprise / governed / execution language; results lead with 4x
throughput; path and About name governance and the patent-pending
workflow. Banned hype words stayed out. Numbers unchanged.

Pushed f3aee55 to origin/main as amoghd3. Pages should redeploy at
https://amoghd3.github.io/portfolio/

Moved workspace — 2026-09-07

Copied the repo from C:\Users\Blese\portfolio to
C:\Users\Blese\OneDrive\Desktop\development\portfolio.

development already had other projects and its own .git, so the site
stays in a portfolio subfolder with its own remote
(https://github.com/amoghd3/portfolio.git). Cursor workspace should
point at the new path. The old C:\Users\Blese\portfolio folder can be
deleted once Cursor releases the lock.

Local preview — 2026-09-07

Port 4173 had leftover Python servers that returned empty replies.
Killed those PIDs and started python -m http.server 5500 --bind 127.0.0.1
from the new portfolio folder. Home page returns 200 at
http://127.0.0.1:5500/ (title: Amogh Donthi, AI Product Strategist).

Work accordion — 2026-09-07

Work nodes on index.html and work.html now expand in place. Click a
node to open that project's map, step callout, and How it was built /
scaled / features. Click another node and the first panel closes.
Click the open node again to collapse it. No panel is open on load
unless the URL hash names a project (index.html#ducky, work.html#orbit).
app.js no longer defaults to Prompt Studio.

Ducky + Excel harness merged — 2026-09-07

Removed the separate Excel AI harness node. Ducky AI is one system:
RFx generation that runs in Excel through a VBA harness. The map now
has Build (corpus, ask, draft), Execute (harness, run in sheet, write
results), and Ship (edit, cheaper/faster, Salesforce). Copy states the
harness is how RFx work was drafted, acted on, and executed cheaper
and faster. 90% and 83% numbers unchanged. #excel still opens Ducky.

Push — 2026-09-07

Committing the Work accordion, Ducky/Excel merge, and human-writing
rule, then pushing main to origin.
